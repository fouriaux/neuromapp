#include "cable_section/options.h"

static double V[];
static double I[];

static double t;          // to be initialized in configure
static double delta_t;    // to be initialized in configure
static double delta_x     // to be initialized in configure

int cable_section_configure (int argc, char** argv) {


}


/*
    solving 
 
 
 
 */

int execute () {
// Writting I (input of other solvers)


// Writting V (output of analytical solution to compare)


// Virtual increment time step
current_time += time_step;
}
